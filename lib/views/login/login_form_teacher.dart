import 'package:flutter/material.dart';

class LoginFormTeacher extends StatelessWidget {
  const LoginFormTeacher({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
